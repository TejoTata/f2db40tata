extends layout

block content
  h1= title
  -
  - let result=results
  div(class="container-fluid")
     div(class='row m-1')
        each item,index in result
            -let destination = `dispatch(\"/vehicle/update?id=${item._id}\")`
            div.col-sm-2(onclick=`buy(${index})`) vehicle Name: #{item["vehicle_name"]}  <br>  vehicle_type: #{item["vehicle_type"]} <br> vehicle_price: #{item["vehicle_price"]} 
            button.btn(class="btn-primary ml-1", onclick = destination) Update
  script.
      function buy(index){
         alert("you purchased item "+(index+1))
      }
  script.
      function dispatch(path){
      window.location.href=path
      }
   //- button(type='button',value='ok') ok
   //- button(type='button',value='save') save
   //- button(type='button',value='update') update
   //- button(type='button',value='delete') delete